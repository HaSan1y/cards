# conflicts netlify.toml ?
# into your publish directory public/, dist/, build/, or output/

# Redirect from / to public folder, status !force, 301! (permanent), 302 (temporary), 200 (rewrite), 404 (custom 404), 200 same url-directory

/  /index.html   200
